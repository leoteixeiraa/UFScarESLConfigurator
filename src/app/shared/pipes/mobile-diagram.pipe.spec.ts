import { MobileDiagramPipe } from './mobile-diagram.pipe';

describe('MobileDiagramPipe', () => {
  it('create an instance', () => {
    const pipe = new MobileDiagramPipe();
    expect(pipe).toBeTruthy();
  });
});
