import { WebDiagramPipe } from './web-diagram.pipe';

describe('WebDiagramPipe', () => {
  it('create an instance', () => {
    const pipe = new WebDiagramPipe();
    expect(pipe).toBeTruthy();
  });
});
